#include "main.h"

/**
 * mul -  function that multiplies two integers.
 * @a: first input
 * @b: second input
 * Return: Multiplication result of the two inputs
 */
int mul(int a, int b)
{
	return (a * b);
}
